"""Unattended pull-request reviewer.

The script does the work; the model is called once per review, on input the
script has already filtered, and only when something has actually changed.
"""

__version__ = "0.1.0"
